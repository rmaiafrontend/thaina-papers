{%- liquid
  assign blocks = section.blocks
-%}

<section class="tp-section tp-portfolio">
  <div class="tp-section-head">
    {%- if section.settings.heading != blank -%}
      <h2 class="tp-heading">{{ section.settings.heading }}</h2>
    {%- endif -%}
    {%- if section.settings.link_label != blank -%}
      {%- assign destino = section.settings.link | default: routes.root_url -%}
      {%- comment -%}
        Link externo abre em aba nova: quem está vendo os trabalhos não
        deve perder a navegação da loja ao conferir o perfil.
      {%- endcomment -%}
      <a
        href="{{ destino }}"
        class="tp-link-caps"
        {% if destino contains '://' %}
          target="_blank"
          rel="noopener"
        {% endif %}
      >
        {{- section.settings.link_label -}}
      </a>
    {%- endif -%}
  </div>

  <div class="tp-portfolio__grid">
    {%- for block in blocks -%}
      {%- assign position = forloop.index -%}
      <div class="tp-portfolio__cell tp-portfolio__cell--{{ position }}" {{ block.shopify_attributes }}>
        {%- render 'tp-image',
          image: block.settings.image,
          asset: block.settings.image_asset,
          alt: block.settings.caption,
          class: 'tp-portfolio__media',
          ratio: '2/3',
          width: 1200,
          sizes: '(max-width: 899px) 84vw, 25vw',
          widths: '300,500,700,1000,1200'
        -%}
        {%- if block.settings.caption != blank -%}
          <div class="tp-portfolio__caption">{{ block.settings.caption }}</div>
        {%- endif -%}
      </div>
    {%- endfor -%}
  </div>
</section>

{% stylesheet %}
  .tp-portfolio {
    padding-block: 0 84px;
  }

  /* Fileira de retratos, não mosaico deitado. O material da Thainá é foto de
     celular em pé — bolo inteiro, mesa posta, convite na mão. No mosaico
     antigo, de células deitadas, toda foto virava uma tira cortada no meio. */
  /* Quatro colunas, não cinco. O container do site tem 1240px e, num laptop
     comum, ele já é a própria largura da tela — então tirar o teto não faz a
     foto crescer um pixel. Quem manda é o número de colunas: de cinco para
     quatro, o card vai de 235 para 298 de largura. Uma foto a menos, as
     outras quatro 27% maiores. */
  .tp-portfolio__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    align-items: start;
  }

  /* 2:3 em vez de 3:4: as fotos da Thainá são todas de celular em pé, e o
     quadro mais alto devolve o que o corte anterior tirava — o bolo inteiro,
     o convite com os nomes do casal. Mais alto também é mais presente na
     página, sem precisar tirar foto da fileira. */
  .tp-portfolio__media {
    aspect-ratio: 2 / 3;
  }

  .tp-portfolio__caption {
    font-size: 12px;
    line-height: 1.45;
    color: var(--tp-muted);
    margin-top: 10px;
  }

  @media (max-width: 899px) {
    .tp-portfolio {
      padding-block: 0 34px;
    }

    /* No celular vira carrossel horizontal. Cinco retratos empilhados em duas
       colunas viravam uma parede de rolagem; deitados numa faixa, o dedo
       percorre os trabalhos como quem passa fotos.

       Quem rola é a própria grade, não a página — a faixa sangra até a borda
       da tela e reaproveita o gutter que .tp-section > * já aplica como folga
       das pontas, então não precisa de margem negativa. */
    .tp-portfolio__grid {
      grid-template-columns: none;
      grid-auto-flow: column;
      grid-auto-columns: 82%;
      gap: 12px;
      overflow-x: auto;
      overscroll-behavior-x: contain;
      scroll-snap-type: x mandatory;
      scroll-padding-inline: var(--tp-gutter);
      scrollbar-width: none;
    }

    .tp-portfolio__grid::-webkit-scrollbar {
      display: none;
    }

    .tp-portfolio__cell {
      scroll-snap-align: start;
      min-width: 0;
    }

  }
{% endstylesheet %}

{% schema %}
{
  "name": "TP — Trabalhos reais",
  "tag": "section",
  "max_blocks": 5,
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Título",
      "default": "Trabalhos reais"
    },
    {
      "type": "text",
      "id": "link_label",
      "label": "Texto do link",
      "default": "Ver portfólio"
    },
    {
      "type": "url",
      "id": "link",
      "label": "Link"
    }
  ],
  "blocks": [
    {
      "type": "work",
      "name": "Imagem",
      "settings": [
        {
          "type": "text",
          "id": "image_asset",
          "label": "Arquivo no tema (fallback)",
          "info": "Usado só quando nenhuma imagem é escolhida."
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Imagem"
        },
        {
          "type": "text",
          "id": "caption",
          "label": "Legenda do espaço vazio"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "TP — Trabalhos reais",
      "blocks": [
        { "type": "work" },
        { "type": "work" },
        { "type": "work" },
        { "type": "work" },
        { "type": "work" }
      ]
    }
  ]
}
{% endschema %}
